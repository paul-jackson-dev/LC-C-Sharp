@using CodingEvents.Models
@model List<Tag>

    <h1>Tags</h1>

@if(Model.Count == 0)
{
    <h2>No tags yet</h2>
}
else{
    <li>
        @foreach (Tag tag in Model)
        {
        <ul>@tag.Id : @tag.Name</ul>
        }
    </li>
}